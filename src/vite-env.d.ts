/// <reference types="vite/client" />

/** Build timestamp injected by `define` in `vite.config.ts` — shown on Profile
 *  so a headset can be checked against the build you meant to deploy. */
declare const __BUILD_STAMP__: string
